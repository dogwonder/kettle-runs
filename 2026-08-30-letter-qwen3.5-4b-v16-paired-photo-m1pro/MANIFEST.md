# 2026-08-30 — app.kttl.letter-to-actions, 4B, scoring v16, paired bed, photo arm

- **Pack**: app.kttl.letter-to-actions 0.3.0
- **Tree**: kettle `f775c3e` (branch `fix/photo-layout-resolution`, PR #584); prompts unchanged from main `8e58b27`
- **Model**: qwen3.5-4b-q4_k_m.gguf (4B, Q4_K_M, context 8192), the file `app/src-tauri/models.json` names
- **Eval set**: `--fixture-dir` — the 22 letters of kettle-examples out-photos (JPEG), read by Vision; kettle-examples `fd39dc9`. 22 fixtures, 29 obligation decisions. The fixture ids are the directory names under `run1/`.
- **Scoring version**: 16
- **Bed digest**: none printed for a `--fixture-dir` run; the bed is the enumerated fixture list, regenerable from kettle-examples at the commit above
- **Sidecar**: llama-server b10145, macOS arm64, Metal
- **Machine**: Apple M1 Pro, 32 GB
- **Runtime**: context 8192, reasoning off
- **Verdict**: FAIL (both gates; the ungated obligation-precision loss is the parked #399 conditional/advisory work)
- **Result**: photographs, after the layout fix: 21/29 obligation decisions exact (recall 0.59 reported); before the fix on the same bed, 14/29 (0.48)
- **Backs**: the 30 August 2026 finding recorded on #474 and in kettle's `CHECKLIST.md` — the photo penalty was structural reading in Rust, not the model: 7 of 11 lost decisions resolved after a correct answer once the segmenter cut a page by its columns. This is a `--fixture-dir` run archived under the retention rule's second clause: a cited finding stands on it.

## Provenance notes

Bed fixtures only: every letter is wholly synthetic, generated by
kettle-examples (invented names, addresses, references and amounts). No
`*.private.*` input, no OCR or transcript of a real document.

The run directory kettle wrote (`evals/runs/run1`) is reused across
runs and held 935 entries from earlier sittings; only the 22
recordings of this arm's fixtures are archived, copied unchanged.
